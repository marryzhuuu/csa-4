# config.py — общие константы для компилятора и эмулятора JL / M68k

# Memory-mapped IO ports (в пространстве данных dmem)
IO_IN = 0xFFFF0000  # порт ввода:  чтение возвращает следующий токен (-1 при EOF)
IO_OUT = 0xFFFF0004  # порт вывода: запись добавляет токен в буфер вывода

# Выходная директория по умолчанию для компилятора
DEFAULT_OUT_DIR = "debug"

# Ограничение длины лога по умолчанию
DEFAULT_TRACE_LIMIT = 200
